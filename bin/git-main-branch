#!/usr/bin/env fish
function _has_ref --argument refname
    # git show-ref exits 0 if ref exists. Suppress output.
    git show-ref --verify --quiet $refname
end

if _has_ref refs/remotes/origin/master or _has_ref refs/heads/master
    echo master
else
    echo main
end
